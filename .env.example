# Runtime settings are now config-file driven.
# Use `config/runtime.toml` (and optional `config/runtime.local.toml`) instead of `.env`.
#
# Keep this file only if other local tooling needs it.
ODDS_API_KEY=
OPENAI_API_KEY=
